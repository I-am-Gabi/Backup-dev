#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
	srand((unsigned)time(NULL));
	long long int p, n, q;
	int i, j;
	int randon = 1;
	while !((pow(2,randon-1))%randon == 1) {
		randon = rand();
	}
	n = p * q;
}